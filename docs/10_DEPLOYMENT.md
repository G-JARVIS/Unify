# DevOps & Cloud Deployment

- **Containerization**: Multi-stage `Dockerfile` for Frontend and FastAPI microservices.
- **Orchestration**: `docker-compose.yml` for local development (PostgreSQL + Redis + FastAPI + React Frontend).
- **CI/CD**: GitHub Actions workflow for linting, type checks, pytest runs, and deployment.